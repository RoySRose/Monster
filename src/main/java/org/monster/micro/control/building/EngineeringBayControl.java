package org.monster.micro.control.building;

import bwapi.Unit;
import org.monster.common.UnitInfo;
import org.monster.micro.control.Control;

import java.util.Collection;

@Deprecated
public class EngineeringBayControl extends Control {
    @Override
    public void control(Collection<Unit> unitList, Collection<UnitInfo> euiList) {

//        for (Unit unit : unitList) {
//            if (skipControl(unit)) {
//                continue;
//            }
//
//            buildingFlyMap.put(unit, new FlyCondition(true, false, null));
//
//            if (TimeUtils.getFrame() > 12000) {
//                buildingFlyMap.get(unit).setFlyPosition(getFlyPosition0(unit));
//            }
//
//            processFly(unit);
//        }
    }

//    @Override
//    public void checkFlyCondition(Unit unit) {
//
//    }
}
